{
  "name": "nextra-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "next build --webpack",
    "dev": "if command -v claim-port >/dev/null 2>&1; then claim-port 4345 || exit 1; fi; next dev --webpack --port 4345",
    "start": "if command -v claim-port >/dev/null 2>&1; then claim-port 4345 || exit 1; fi; next start --port 4345"
  },
  "dependencies": {
    "@diagrammo/dgmo": "0.60.0",
    "next": "^16.0.0",
    "nextra": "^4.0.0",
    "nextra-dgmo": "link:../..",
    "nextra-theme-docs": "^4.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remark-dgmo": "0.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^6.0.2"
  },
  "pnpm": {
    "overrides": {
      "zod": "4.1.12"
    }
  },
  "engines": {
    "node": ">=20.6"
  }
}
